-- Feature boundary for two independent skin families. When both are enabled, the higher
-- bonus wins for each building instead of stacking.
return {
	GooSkinsEnabled = true,
	BuildingSkinsEnabled = false,
	CombineBonuses = "Max",
}
