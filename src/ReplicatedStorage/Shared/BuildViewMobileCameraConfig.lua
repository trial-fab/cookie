-- Independent defaults for the touch-first Build View camera. These deliberately do
-- not alias the desktop constants: phone tuning can move freely without changing PC.
local BuildViewMobileCameraConfig = {
	DEFAULT_FOV = 60,
	PITCH_DEGREES = 55,
	MIN_PITCH_DEGREES = 15,
	MAX_PITCH_DEGREES = 85,
	PLACEMENT_MARGIN_STUDS = 18,
	ENTRY_FRAME_SCALE = 1.18,
	ROAM_SLACK_STUDS = 0,
	CAMERA_STANDOFF_STUDS = 30,
	MIN_HEIGHT = 2,
	CEILING_ALLOWANCE = 160,
	BOUNDS_TAU = 0.1,
	MIN_DISTANCE = 16,
	MAX_DISTANCE = 2200,
	EDGE_PAN_ZONE_PX = 72,
	EDGE_PAN_SPEED = 72,
	VERT_SPEED = 48,
	PAN_MOMENTUM_SECONDS = 0.35,
	PAN_MOMENTUM_SCALE = 0.55,
	MIN_FLING_PX_PER_SEC = 240,
	MAX_FLING_PX_PER_SEC = 1200,
	MIN_FLING_TRAVEL_PX = 24,
	PAN_VELOCITY_SMOOTHING = 0.35,
	MOMENTUM_RELEASE_WINDOW = 0.15,
	PAN_REFERENCE_PITCH_DEGREES = 55,
	PAN_MAX_STUDS_PER_PIXEL = 0.5,
	PINCH_SENSITIVITY = 0.45,
	TWIST_SENSITIVITY = 0.75,
	PITCH_DRAG_SENSITIVITY = 0.0035,
	PITCH_DEAD_ZONE_PX = 12,
}

return table.freeze(BuildViewMobileCameraConfig)
