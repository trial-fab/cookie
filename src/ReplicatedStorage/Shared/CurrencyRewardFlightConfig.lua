-- Baked reward-flight presentation values. These were promoted from the final live-tuned
-- CurrencyRewardFlight session and are now the single runtime source of truth.

return {
	SourceRiseSeconds = 0.35,
	SourceRisePixels = 100,
	SourcePauseSeconds = 0.4,
	FlightSeconds = 0.6,
	WaypointProgress = 0.35,
	WaypointHeightScale = 0.2,
	GhostCount = 6,
	GhostDelaySeconds = 0.01,
	LandingPulseScale = 1.15,
	LandingPulseSeconds = 0.4,
	CountUpSeconds = 1.5,
	BatchWindowSeconds = 0.5,
	EventMatchSeconds = 0.5,
	LandingLabelHoldSeconds = 1.5,
	LandingLabelFadeSeconds = 0.4,
	StoreRevealSeconds = 0.18,
	StorePostLandingHoldSeconds = 0.7,
}
